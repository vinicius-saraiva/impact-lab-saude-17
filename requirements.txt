# Hackathon RJ — Claude Impact Lab 24/05/2026
# Stack: Streamlit + análise de dados + geo + DataSUS

# UI / Dashboard
streamlit>=1.35.0
streamlit-folium>=0.20.0

# Dados e análise
pandas>=2.2.0
numpy>=1.26.0
requests>=2.31.0
duckdb>=1.0.0
pyarrow>=15.0.0

# Visualização
plotly>=5.22.0
folium>=0.17.0

# Geo
geopandas>=0.14.0

# DataSUS — pysus/readdbc requerem C++ Build Tools no Windows
# Alternativa: baixar CSVs pré-convertidos do TabNet manualmente
# pysus>=0.14.0   # não disponível para Python 3.14 sem MSVC
# readdbc>=0.2.0  # idem

# Utilitários
python-dotenv>=1.0.0
openpyxl>=3.1.0
